# Review Findings — PR #77 (Fonts-1: font model, embedding & FontFace loading)

**Date:** 2026-06-12 · **Branch:** feat/fonts-1 · **HEAD reviewed:** 52dfa25
**Reviewers:** Architect, Security, BE, Logic, Compliance, Data Scientist, FE, A11y, UX, DevOps (10, parallel, scoped diffs)
**Outcome:** No Critical. 1 High (RF-001). Security: APPROVE. Overall: strong; merge after RF-001 + agreed Medium/Major items.

| ID | Severity | Source | Status | Finding | Recommended fix |
|----|----------|--------|--------|---------|-----------------|
| RF-001 | High | DataSci DF-1, DevOps DV-1 | open | No aggregate cap on total embedded font bytes per session (32 MiB × 256 = ~8 GiB resident); memory-exhaustion/DoS in container target. | `MAX_TOTAL_EMBEDDED_FONT_BYTES` + running-sum enforcement at addFont (server+MCP) and load_font_assets, symmetric, `_enforced` test. |
| RF-002 | Major | UX UF-2, FE FF-4, A11y AY-2 | open | Embed-vs-reference classification never surfaced; restricted font shows identical "Added X". | addFont returns FontEntry; helper branches on `embeddable`, uses canonical `family`. |
| RF-003 | Major | UX UF-1 | open | No visible feedback for sighted users (sr-only role=status only); §11 wants visible toast/inline too. | Route add result + typed error to Toast / inline error. |
| RF-004 | Medium | Logic LF-1 | open | validate/apply asymmetry: font with no Unicode family record passes validate, fails apply (empty family). | validate runs FontEntry::new / validate_font_family_name on parsed family. |
| RF-005 | Medium | FE FF-1, Architect AF-3 | open | Orchestrator processedIds never pruned; remove-then-re-add same id never reloads. | Reconcile processedIds vs current key-set each run; test. |
| RF-006 | Medium | FE FF-2 | open | removeFont never deletes the registered FontFace from document.fonts (leak + shadowing). | Track faces per id in font-face-loader; unloadFont(id) from remove paths. |
| RF-007 | Medium | BE BF-1 | open | sigil_state::BROADCAST_PATH_FONT_ENTRY unused; both transports hardcode the literal. | Use the constant at both sites; assert in parity tests. |
| RF-008 | Medium | BE BF-2, Compliance CF-1 | open | Tautological `_enforced` test (validate.rs test_max_font_bytes_base64_len_enforced); real enforcement at transports. | Rename to `..._formula`. |
| RF-009 | Low | DataSci DF-2, DevOps DV-2 | open | base64 addFont holds ~3 copies of a 32 MiB font; avoidable .clone() into the command. | Pass ownership; source store bytes from doc. |
| RF-010 | Low | Security SF-1 | open | ctx.font interpolates family unquoted; multi-word families mis-resolve (no injection). | Emit `...px "${family}"`. |
| RF-011 | Low | Logic LF-2 | open | resolve_decision bitmap-only has_outlines omits CFF2. | Add `|| face.tables().cff2.is_some()`. |
| RF-012 | Low | A11y AY-1 | open | Focus not restored to the Add-font button after the native file dialog. | Button ref + .focus() post-handler. |
| RF-013 | Low | BE BF-3 | open | FontAxis derive-Deserialize, public f32, no self-contained finite guard. | Document via-FontEntry-only or add validating ctor. |
| RF-014 | Low | Architect AF-2 | open | Custom entry with bytes dropped at save → reload-time silent "missing", no warning. | Warn for any Custom entry uuid absent from loaded font_bytes. |
| RF-015 | Low | UX UF-4 | open | Dev-facing error messages (addFont: prefix, raw bytes). | User-facing copy in MB; strip debug prefix. |
| RF-016 | Info | Compliance CF-2 | open | Commit 851b605 labeled refactor introduces a type/signature change. | Squash title = feat (no history rewrite). |
| RF-017 | Info | DevOps DV-3 | open | uuid v5 reaches server via cross-crate feature unification. | Add "v5" to workspace/server uuid features. |
| RF-018 | Info | Architect AF-4, UX UF-5 | open | Resource-import undo model undocumented in spec deferred table. | One line in spec deferred inventory. |

## Notes
- Security APPROVE: untrusted-font-parse surface (ttf-parser, size caps at every boundary incl. pre-base64, uuid-only filenames, CSS denylist input+output, manual validating Deserialize, typed migration errors) is layered and tested. One pre-existing non-blocking gap: no WS message-size cap (SF-2) — Fonts-1 sends no bytes over WS.
- Verified-clean by multiple reviewers: WASM audit (ttf-parser no_std, uuid v5 = sha1_smol, no Send/Sync/'static added to core); §10 migration persistence contract (force-persist + byte-identical .backup-v{N} + migrated_from, CI smoke test); broadcast parity GraphQL↔MCP (shared sigil_state consts, id in create broadcasts); validated-type discipline; exhaustive discriminated-union dispatch; parity fixture; lock/seq-under-lock ordering.
