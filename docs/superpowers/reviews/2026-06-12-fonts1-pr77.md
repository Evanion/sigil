# Review Findings — PR #77 (Fonts-1: font model, embedding & FontFace loading)

**Date:** 2026-06-12 · **Branch:** feat/fonts-1 · **HEAD reviewed:** 52dfa25
**Reviewers:** Architect, Security, BE, Logic, Compliance, Data Scientist, FE, A11y, UX, DevOps (10, parallel, scoped diffs)
**Outcome:** No Critical. 1 High (RF-001). Security: APPROVE.
**Remediation:** all 15 code-actionable findings RESOLVED in commits `34036be` (Rust) + `43964d9` (frontend); RF-018 documented in spec. Full CI-equivalent gate green (Rust clippy/test/fmt/wasm; frontend tsc/test/lint/format/locale-parity).

| ID | Severity | Source | Status | Finding | Resolution |
|----|----------|--------|--------|---------|-----------|
| RF-001 | High | DataSci DF-1, DevOps DV-1 | resolved | No aggregate cap on total embedded font bytes/session (~8 GiB). | `MAX_TOTAL_EMBEDDED_FONT_BYTES`=256 MiB + running-sum enforcement at addFont (GraphQL+MCP, check-before-apply) and load_font_assets (warn+skip), `_enforced` tests symmetric across transports. |
| RF-002 | Major | UX UF-2, FE FF-4, A11y AY-2 | resolved | Embed-vs-reference classification never surfaced. | store.addFont returns the FontEntry; helper branches on `embeddable` via `addFontSuccessMessage` (never-sentinel), uses canonical `family`; `fontAddedReferenced` key (en/es/fr). |
| RF-003 | Major | UX UF-1 | resolved | No visible feedback (sr-only only). | Result/error routed to the Toast component (success/warning/error) alongside the persistent role=status region. |
| RF-004 | Medium | Logic LF-1 | resolved | validate/apply asymmetry (no-Unicode-name font). | AddFontEntry validate+apply share `build_entry()` → both route FontEntry::new; symmetry test added. |
| RF-005 | Medium | FE FF-1, Architect AF-3 | resolved | Orchestrator processedIds never pruned. | Prune processedIds vs current key-set each run; add→remove→re-add test. |
| RF-006 | Medium | FE FF-2 | resolved | removeFont never deletes the FontFace. | font-face-loader tracks faces by id + `unloadFont(id)`; called from removeFont + applyRemoveFont; tests. |
| RF-007 | Medium | BE BF-1 | resolved | BROADCAST_PATH_FONT_ENTRY unused. | Both transports use the shared const; parity tests assert op.path == const. |
| RF-008 | Medium | BE BF-2, Compliance CF-1 | resolved | Tautological `_enforced` test. | Renamed to `test_max_font_bytes_base64_len_formula`. |
| RF-009 | Low | DataSci DF-2, DevOps DV-2 | resolved | Redundant 32 MiB clone in addFont. | Build op by move; reclaim op.bytes for font_bytes insert after apply (no up-front clone). |
| RF-010 | Low | Security SF-1 | resolved | ctx.font family unquoted. | buildFontString emits `...px "${family}"`; tests updated. |
| RF-011 | Low | Logic LF-2 | resolved | resolve_decision omits CFF2. | Added `|| face.tables().cff2.is_some()`. |
| RF-012 | Low | A11y AY-1 | resolved | No focus restore after file dialog. | Button ref + .focus() post-handler; tests (selected + cancelled). |
| RF-013 | Low | BE BF-3 | resolved | FontAxis no self-contained guard. | Doc-comment states the via-FontEntry-only invariant + future-deserializer obligation. |
| RF-014 | Low | Architect AF-2 | resolved | Custom entry missing bytes silent on load. | load_workfile warns for any Custom entry absent from loaded font_bytes. |
| RF-015 | Low | UX UF-4 | resolved | Dev-facing error copy. | User strings (MB via fontTooLarge, fontAddFailed); addFont:/byte detail stays in console.error. |
| RF-016 | Info | Compliance CF-2 | squash-time | Commit 851b605 labeled refactor introduces a type/signature. | Repo squash-merges; squash title is `feat` — fixed at squash, no history rewrite. |
| RF-017 | Low | DevOps DV-3 | resolved | uuid v5 via cross-crate feature unification. | Added `"v5"` to workspace uuid features (root Cargo.toml). |
| RF-018 | Info | Architect AF-4, UX UF-5 | resolved | Resource-import undo model undocumented. | Added to the spec Non-Goals/deferred section. |

## Notes
- Security APPROVE: untrusted-font-parse surface (ttf-parser, size caps at every boundary incl. pre-base64, uuid-only filenames, CSS denylist input+output, manual validating Deserialize, typed migration errors) is layered and tested. Pre-existing non-blocking gap: no WS message-size cap (SF-2) — Fonts-1 sends no bytes over WS; tracked separately.
- Verified-clean: WASM audit; §10 migration persistence contract (force-persist + byte-identical .backup-v{N} + migrated_from, CI smoke test); broadcast parity GraphQL↔MCP (shared sigil_state consts, id in create broadcasts); validated-type discipline; exhaustive discriminated-union dispatch; parity fixture; lock/seq-under-lock ordering.
